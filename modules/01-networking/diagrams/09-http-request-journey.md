# HTTP Request Journey

```text
Browser
   │
HTTP Request
   │
TCP Segment
   │
IP Packet
   │
Ethernet Frame
   │
Bits
──────── Internet ────────
Bits
   │
Ethernet Frame
   │
IP Packet
   │
TCP Segment
   │
HTTP Request
   │
Web Server
```

Illustrates encapsulation and decapsulation for a web request.
